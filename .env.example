# SQL Explorer key for the Hyperliquid cluster. Server-side only; never shipped to the browser.
QN_SQL_API_KEY=
# Quicknode Hyperliquid endpoint base URL (the route appends /info), used for the venue candleSnapshot check.
QN_HYPERCORE_URL=https://<name>.hype-mainnet.quiknode.pro/<token>
